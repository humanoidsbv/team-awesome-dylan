import React from 'react';

import styles from './NewTimeEntry.module.css';

const NewTimeEntry = () => (
  <button type="button" className={styles.NewTimeEntry}>
    + New Time Entry
  </button>
);

export default NewTimeEntry;
